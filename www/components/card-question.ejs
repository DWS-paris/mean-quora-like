<section>
    <article class="userQuestion">
        <p class="questionAuthor"><i class="far fa-question-circle"></i> <span>Julien Noyer</span></p>
        <h2><%= data.question.headline %></h2>
    </article>
    <% if(data.comments.length > 0) { %>
        <article class="userResponse">
            <h3><i class="fas fa-comments"></i>  Réponses des utilisateurs</h3>
            <ul>
                <% data.comments.map(item => { %>
                    <li><%- include('./card-comment.ejs', { item: item }); %></li>
                <% }) %>
            </ul>
        </article>
    <% } %>
</section>