<%- include('./static/header.ejs') %>

<main class="contentPage">
    <section id="mainQuestion">
        <%- include('./components/header-question.ejs', { data: data }); %>
        <div class="headlineQuestionPublic"><%= data.question.headline %></div>
    </section>
    <div>
        <section class="grid response">
            <% if( data.responses.length > 0 ) { %>
                <% for( let item of data.responses ) { %>
                    <%- include('./components/card-response.ejs', { data: item, parent: data.question._id }); %>
                <% } %>
            <% } %>
            <% if(data.responses.length < 2) { %>
                <aside></aside>
            <% } %>
            <% if(data.responses.length < 3) { %>
                <aside></aside>
            <% } %>
        </section>
    </div>
</main>

<% include ./static/footer.ejs %>